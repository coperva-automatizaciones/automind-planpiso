/* Automind · usuarios.jsx — funcionalidad integrada en colaboradores.jsx
   Este archivo se mantiene solo para compatibilidad de referencias. */
function RegistroUsuarios() { return null; }
Object.assign(window, { RegistroUsuarios });
